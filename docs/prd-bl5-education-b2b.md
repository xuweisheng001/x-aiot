# 教育与 B 端 AIoT 产品需求文档

> Product Requirements · BL5 教育与 B 端

xTool 设备云 BL5 业务线的产品需求：让学校与工作室以「组织」而不是「个人」管理一批机器。教室多机看板、上课时段外禁用、批量 OTA、按班级任务队列、工作室多机调度与耗材集采六个功能域，全部复用 BL1 建成的接入、影子、事件、指令、OTA 五条链路与 BL4 的 App / XCS 入口、参数库、云端下发任务，只新增组织租户模型、策略下发与任务队列三块。BL5 整体处于平台 P2 阶段，本文内部再分 P0 试点、P1 扩展、P2 商业化三档。与《xTool AIoT 平台技术方案》§1 / §6 / §12、BL4 技术方案 §4 / §6.4 / §10 及工程仓库 x-aiot 一一对应。

**优先级图例**

| 标记 | 含义 |
|---|---|
| **P0 必须** | 首批 3 到 5 所试点学校上线前必须交付 |
| **P1 应该** | 试点后首个迭代，覆盖工作室与集采 |
| **P2 可以** | B 端订阅商业化，需 BL4 entitlement 与支付就位 |

**文档信息**

| 项 | 内容 |
|---|---|
| 文档版本 | v0.1 评审稿 · 2026-09-18 |
| 业务线 | BL5 教育与 B 端（学校、培训机构、小微工作室），复用 BL4 入口与 BL1 平台 |
| 产品目标 | 一个老师管一间教室的机器不用挨台看；一个工作室主管十几台机器不用挨台点 |
| 北极星指标 | B 端付费账号 · 单校设备数 · 续费率 |
| 关联文档 | docs/prd-bl1-consumer-laser.md · docs/prd-bl4-software-content.md · docs/tech-design-aiot-platform.md · docs/tech-design-bl4-software-content.md |
| 状态 | 待 3 所试点学校签署试点协议后立项；商业化部分待 BL4 entitlement 上线 |

## 目录

1. [背景与目标](#01-背景与目标)
2. [用户、场景与用户故事](#02-用户场景与用户故事)
3. [范围：P0 做什么、不做什么](#03-范围p0-做什么不做什么)
4. [功能需求](#04-功能需求)
5. [客户端与设备端需求](#05-客户端与设备端需求)
6. [数据需求](#06-数据需求)
7. [非功能需求与 SLO](#07-非功能需求与-slo)
8. [埋点与指标口径](#08-埋点与指标口径)
9. [里程碑与验收出口](#09-里程碑与验收出口)
10. [依赖、风险与开放问题](#10-依赖风险与开放问题)

---

## 01 背景与目标

### 1.1 问题

- 一间创客教室通常有 6 到 20 台激光机，老师上课时要同时盯所有机器的状态与安全，下课后要确保没人偷开机器。今天这些全靠人走动巡视和钥匙锁门。
- 学校采购后固件版本很快碎片化，老师不会也不敢逐台升级；同一节课不同机器参数表现不一致，教学无法标准化。
- 学生提交加工任务靠 U 盘和排队喊号，机器空转与拥堵并存；老师没有审批环节，学生可能切不允许的材料。
- 工作室主每天要在十几台机器间分派任务、盯耗材，激光模块什么时候该换只能靠经验；集中采购耗材没有数据依据。
- 设备云现有模型以「个人绑定」为中心，一台机器属于一个用户。学校的机器属于学校，老师会换、学生每年换一批，个人绑定模型无法表达。

### 1.2 目标

1. **组织**：设备归属组织而不是个人；组织内按教室或车间分组，成员按角色授权；老师离职、学生毕业不影响设备归属。
2. **可见**：一张看板看全组织所有机器的在线、作业、告警、待升级状态，告警仍推送到具体责任人而不是只在看板上闪。
3. **可控**：上课时段外机器自动禁用，断网也生效；解锁需要老师授权；禁用绝不影响安全功能与急停。
4. **高效**：任务按班级排队、老师审批、机器空闲即下发；批量 OTA 只在维护窗口执行且不绕过任何灰度与熔断护栏。
5. **数据**：组织级耗材健康度汇总生成集采清单，替代经验判断。

### 1.3 北极星与护栏指标

| 指标 | 定义 | 目标 | 类型 |
|---|---|---|---|
| B 端付费账号 | 有有效订阅的组织数 | P2 首年 200 | 北极星 |
| 单校设备数 | 组织下已归属设备数的中位数 | P0 试点 ≥ 8；P1 ≥ 12 | 北极星 |
| 续费率 | 到期组织中 90 天内续费的比例 | P2 ≥ 70% | 北极星 |
| 看板周活跃率 | 一周内至少打开看板 3 次的组织管理员 / 全部管理员 | ≥ 60% | 护栏 |
| 时段外违规开机率 | 禁用时段内 JOB_START 事件 / 禁用时段总数 | < 1% | 护栏 |
| 队列任务平均等待 | 学生提交到机器开始加工的中位时长 | 试点 ≤ 15 min | 护栏 |
| 禁用误伤安全率 | 禁用期间安全事件未停机或未告警的次数 | 0 | 护栏 |

---

## 02 用户、场景与用户故事

### 2.1 目标用户

- **学校信息化管理员 / 采购负责人**：管几十台设备的归属、账号与固件，一年操作几次但每次都是批量。
- **创客课老师**：每周上课，需要一眼看清教室里所有机器的状态，审批学生任务，下课后确认机器已锁。
- **学生**：只需提交任务、看排队进度与结果，不需要也不应该有任何控制权。
- **工作室主 / 生产主管**：一天十几个订单分派到多台机器，关心谁在忙、谁快坏、耗材什么时候补。

### 2.2 核心用户故事

> **US-1 一张看板** 作为老师，我希望上课时在讲台平板上看到教室里 12 台机器谁在线、谁在切、谁报警、谁该升级，而不用走到每台机器前面看屏幕。

> **US-2 下课自动锁** 作为老师，我希望下课铃响后机器自动进入禁用状态，学生按开始也不会出光；断网了也一样锁着；我需要临时用时能在手机上解锁。

> **US-3 锁不住安全** 作为管理员，我希望禁用只是禁止开始新任务，火焰检测、急停、告警推送在任何状态下都正常工作，我不想为了管理牺牲安全。

> **US-4 排队与审批** 作为学生，我希望在 XCS 里提交任务后看到自己排第几、预计几点轮到；作为老师，我希望能先看一眼学生的材料和参数再批准，不合适就退回。

> **US-5 统一升级** 作为管理员，我希望勾选一批机器在周末凌晨统一升级固件，升级过程遵守灰度与熔断，出问题自动停下并通知我。

> **US-6 集采有据** 作为工作室主，我希望看到每台机器激光模块与滤芯的剩余寿命汇总，导出一张下季度采购清单，而不是等哪台切不透了才买。

> **US-7 人走设备留** 作为管理员，我希望老师离职、学生毕业时只需从组织移除成员，设备归属与历史记录不受影响。

---

## 03 范围：P0 做什么、不做什么

| P0 范围内（试点学校） | 明确不做（留给 P1 / P2） |
|---|---|
| 组织与教室两级结构，org_admin / teacher / student 三个角色 | 跨组织共享设备、集团多校区层级（P1） |
| 设备归属组织；个人绑定与组织归属冲突以组织为准并留痕 | 设备在组织间转移的审批流（P1） |
| 教室看板：≤ 200 台聚合状态，30 s 轮询 | SSE 实时推送（P1）；自定义看板布局（P2） |
| 时段策略：周课表 + 云端 lock desired + 设备本地时间表兜底；教师解锁 | 节假日日历同步、临时调课自动适配（P1） |
| 批量 OTA：按组织圈选 + 维护窗口，复用 ota-svc 灰度熔断 | 组织自定义灰度比例（不提供，平台统一） |
| 班级任务队列：提交、审批、排队、空闲下发，复用 BL4 job_start | 多机自动负载均衡调度（P1，工作室场景） |
| 告警推送到教室责任老师 + 看板聚合 | 告警按班级值日轮转（P2） |
| 组织级耗材健康度只读汇总 | 集采清单导出与供应商对接（P1）；支付（不做） |
| 试点 3 到 5 所学校，US 单区 | B 端订阅与计费（P2，走 BL4 entitlement） |

---

## 04 功能需求

编号 FR-5xx，避免与 BL1 的 FR-xx、BL4 的 FR-4xx 冲突。每条给需求、验收标准与平台能力。

### 4.1 组织、成员与设备归属

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-501 **P0** | 创建组织（org）与下级站点（site，教室 / 车间）；组织有唯一 org_id，站点有 site_id | 两级结构可增删改；删除站点需先移空设备 | fleet-svc org / site 表 |
| FR-502 **P0** | 成员角色 org_admin / teacher / student；邀请通过邮箱或邀请码；一个用户可属于多个组织 | 角色矩阵见技术方案 §4；越权 403 | org_member 表、BFF 鉴权 |
| FR-503 **P0** | 设备归属组织与站点：管理员扫码或输入 SN 归入；归属期间该设备对个人绑定用户只读 | 归属后个人 App 的指令面板置灰并提示「由组织管理」；解除归属恢复 | device_org 表；BFF 授权合并 |
| FR-504 **P0** | 个人绑定与组织归属冲突以组织为准，冲突事件留痕并通知个人绑定用户 | 通知含组织名与联系人 | 审计与通知 |
| FR-505 **P0** | 移除成员不影响设备归属与历史数据；成员移除后其 token 对该组织资源立即失效 | 移除后 5 s 内请求 403 | BFF 授权缓存主动失效 |
| FR-506 **P1** | 设备在组织间转移需双方管理员确认；转移后历史加工记录留在原组织 | 转移审批流；job_record 按 org_id 切分 | fleet-svc 转移流程 |
| FR-507 **P1** | 集团多校区：org 支持一级父组织，父组织管理员可见全部子组织看板 | 父级只读，不可直接下指令 | org.parent_id |

### 4.2 教室多机看板

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-511 **P0** | 站点看板：列出站点全部设备的在线 / 离线、工作状态、进度、告警数、固件版本与待升级标记 | ≤ 200 台一次加载 ≤ 2 s；新鲜度 ≤ 30 s | fleet-svc 批量影子聚合（deviceapi GET /shadow 批量） |
| FR-512 **P0** | 组织看板：各站点汇总计数（在线 / 作业 / 告警 / 待升级）与钻取 | 计数与站点看板明细一致 | 聚合缓存 |
| FR-513 **P0** | 看板上的告警只做聚合展示；推送仍发到站点责任老师与 org_admin，用户可按站点订阅 | 告警推送 P99 < 3 s 不因看板改变 | alarm-svc 不改；订阅关系在 fleet-svc |
| FR-514 **P0** | 看板 30 s 轮询，后台停止；带 ETag | 304 比例 > 80% | BFF |
| FR-515 **P1** | 看板 SSE 实时推送，影子变化 5 s 内到达 | 复用 BL4 P1 SSE | BFF SSE |
| FR-516 **P1** | 看板导出当日设备使用报告（作业时长、任务数、告警数） | CSV 导出 ≤ 10 s | telemetry_1h 与 events 聚合 |

### 4.3 时段策略与禁用

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-521 **P0** | 站点级周课表：按星期与时段定义「允许使用」窗口；窗口外设备进入 locked | 课表可视化编辑；生效延迟 ≤ 60 s | schedule_policy 表；desired lock 下发 |
| FR-522 **P0** | 禁用 = 拒绝开始新任务（本地与云端下发均拒），不中断进行中的任务，不影响急停、安全事件、告警与 OTA | 禁用期间注入 FLAME_DETECTED 停机 ≤ 100 ms 且告警推送正常；急停有效 | 固件 DR-501；云端 job_start 前置校验 |
| FR-523 **P0** | 设备本地缓存课表并按本地时钟兜底：断网时窗口外仍禁用、窗口内仍可用 | 拔网线复测：窗口切换准时 ±60 s | 固件 DR-502；desired 含 schedule 快照 |
| FR-524 **P0** | 教师临时解锁：在 App 对单台或整站点解锁 N 分钟（默认 60，上限 240），到期自动回到课表状态；解锁留痕 | 解锁需 teacher 及以上角色；审计可查 | 指令 unlock（desired lock=false + expires_at） |
| FR-525 **P0** | 个人 desired 与组织策略冲突时以组织为准；冲突写审计并通知个人用户 | 个人用户 PATCH lock 被 403 | BFF 裁决 |
| FR-526 **P1** | 节假日日历与调课：导入学校日历，覆盖周课表 | 日历优先级高于周课表 | schedule_policy.overrides |
| FR-527 **P1** | 违规尝试统计：禁用期间设备本地记录「尝试开始」事件并上报，看板显示 | 事件码 LOCKED_START_DENIED | 物模型事件扩展 |

### 4.4 批量 OTA

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-531 **P0** | 管理员按组织 / 站点勾选设备创建批量升级，指定维护窗口（如周六 02:00 到 06:00） | 窗口外不下发；窗口内未完成的下次窗口继续 | ota-svc 批次 + fleet-svc 圈选适配 + policy.window |
| FR-532 **P0** | 批量升级不绕过平台灰度与熔断：组织批次是平台批次的子集，只有平台已到达该档位的固件才可选 | 组织无法选择未到 10% 档的固件 | ota_batch 档位查询 |
| FR-533 **P0** | 升级只在设备空闲且锁定或窗口内进行；进行中任务延后 | 复用 policy.idle_only | ota-svc |
| FR-534 **P0** | 升级进度看板：每台设备的阶段与失败原因；失败率超阈值批次自动暂停并通知管理员 | 复用 ShouldFuseAbs；通知到 org_admin | ota-svc 熔断 + 通知 |
| FR-535 **P1** | 组织可设置「固件冻结」：考试周不接收任何升级 | 冻结期间 ota 下发跳过该组织设备 | device_org.freeze_until |

### 4.5 班级任务队列

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-541 **P0** | 老师按班级创建队列并绑定站点内若干设备；学生在 XCS 提交任务到队列（文件、材料、参数档） | 提交后队列可见排位与预计时间 | job_queue / queue_item 表 |
| FR-542 **P0** | 老师审批：查看材料、参数、预览图后批准或退回（附原因）；可设置「白名单材料自动批准」 | 退回学生收到通知；自动批准只对白名单材料 | 审批状态机 |
| FR-543 **P0** | 调度：批准的任务按提交时间排队，绑定设备空闲且未锁定时下发；下发复用 BL4 云端下发任务的全部安全约束（空闲校验、sha256、预签名 URL 1 h） | 同一设备同时只有一个进行中任务；下发失败回队列头部重试 ≤ 3 次 | fleet-svc 调度器 → BFF job_start |
| FR-544 **P0** | 公平性：同一学生连续任务之间插入其他学生任务（轮转）；单任务预计超 30 min 需老师二次确认 | 排队算法可解释 | 调度策略 |
| FR-545 **P0** | 超时与取消：任务到机器 10 分钟未开始（学生未到场放料）自动跳过到队尾并通知；学生可取消未开始任务 | 跳过次数 ≥ 2 转人工 | 队列状态机 |
| FR-546 **P1** | 工作室模式：无审批，任务按设备能力（模块功率、工作幅面）自动匹配可用设备，负载均衡 | 匹配规则可配置 | 调度器扩展 |
| FR-547 **P1** | 队列统计：每班级任务数、平均等待、退回率 | 进看板 | 聚合 |

### 4.6 耗材集采与健康度汇总

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-551 **P0** | 组织看板显示每台设备激光模块与滤芯健康度（来自 BL3），只读 | 与个人 App 显示一致 | consumable_health 按 org 聚合 |
| FR-552 **P1** | 集采清单：按预测寿命在未来 90 天内到期的部件汇总数量，导出 CSV | 清单含 SN、部件、预计到期、建议数量 | fleet-svc 导出 |
| FR-553 **P2** | 集采清单一键生成官方商城订单草稿 | 不做支付，只生成草稿 | 商城 API |

### 4.7 B 端订阅（P2）

| 编号 | 需求 | 验收标准 | 平台能力 |
|---|---|---|---|
| FR-561 **P2** | 组织订阅按设备数分档；看板、队列、批量 OTA、集采为付费功能 | 订阅状态多端 5 s 一致 | BL4 entitlement-svc org 维度 |
| FR-562 **P2** | **禁用策略、告警推送、急停、安全事件、单台 OTA 不受订阅影响**；订阅到期只关闭看板与队列的写操作，已排队任务执行完 | 代码层订阅校验只出现在 fleet 路由 | 架构约束（沿 BL4 FR-443） |
| FR-563 **P2** | 到期前 30 / 7 / 1 天提醒管理员；到期后 30 天宽限期只读 | 提醒不在上课时段弹出 | entitlement |

---

## 05 客户端与设备端需求

| 编号 | 需求 | 说明 |
|---|---|---|
| CR-501 **P0** | App 新增「组织」入口：切换个人 / 组织视角；组织视角下显示站点看板、课表、队列审批、成员管理 | 复用 BL4 六界面组件 |
| CR-502 **P0** | XCS 组织登录后显示所属队列与设备可用状态；提交任务到队列而非直接加工（学生角色下本地直连加工按钮隐藏） | 学生角色本地直连是否允许由组织策略决定 |
| CR-503 **P0** | 讲台看板模式：平板横屏全屏、大字号、自动刷新、无写操作 | 只读 token |
| CR-504 **P0** | 组织视角下所有云调用带 org_id；个人视角不带 | 租户隔离在客户端也显式化 |
| CR-505 **P1** | 看板 SSE 断线自动回退 30 s 轮询 | 复用 BL4 CR-02 降级 |
| DR-501 **P0** 红线 | 设备端 locked 状态：拒绝本地与云端开始新任务，**不影响**急停、安全规则、告警上报、OTA 与进行中任务 | 台架测试：locked 下注入火焰 → 停机 ≤ 100 ms；急停有效 |
| DR-502 **P0** 红线 | 设备本地缓存课表（desired.schedule 快照）并按本地时钟切换 locked；断网兜底；RTC 漂移 > 10 min 时以云端最后指令为准并上报 | 拔网线复测窗口切换 |
| DR-503 **P0** | locked 状态在屏幕明确显示「已锁定，联系老师」与解锁到期时间 | 用户可理解 |
| DR-504 **P0** | 事件 LOCKED_START_DENIED（禁用期间尝试开始）与 LOCK_STATE_CHANGED 上报 | 物模型 v1.2 |
| DR-505 **P1** | 教师现场解锁：设备屏幕输入一次性解锁码（云端生成 6 位，10 分钟有效） | 无网时唯一解锁路径 |

---

## 06 数据需求

### 6.1 物模型 v1.2 扩展（在 BL4 v1.1 之上）

| 类别 | 项 | 类型 / 取值 | 说明 |
|---|---|---|---|
| 属性 | lock_state | 0 unlocked / 1 locked / 2 temp_unlocked | 变更即报 |
| 属性 | schedule_version | int，本地缓存课表版本 | 上线时与变更即报 |
| desired | lock | bool | 云端强制锁 / 解锁 |
| desired | lock_expires_at | int64 ms，temp_unlocked 到期 | 与 lock=false 同发 |
| desired | schedule | JSONB 周课表快照 + version + tz | 设备本地兜底 |
| 事件 | LOCKED_START_DENIED / LOCK_STATE_CHANGED | 即时 | 违规统计与审计 |

### 6.2 新增表

| 库 | 表 | 关键列 | 说明 |
|---|---|---|---|
| 全局库 | org | org_id PK, name, type(school/studio), parent_id, region, created_at | 组织 |
| 全局库 | site | site_id PK, org_id, name, tz, created_at | 教室 / 车间 |
| 分片库 | org_member | org_id, user_id, role, invited_by, joined_at, removed_at | 部分唯一 (org_id, user_id) WHERE removed_at IS NULL |
| 分片库 | device_org | sn PK, org_id, site_id, assigned_by, assigned_at, freeze_until | 设备归属，一台设备同时只属一个组织 |
| 分片库 | schedule_policy | id, site_id, version, weekly JSONB, overrides JSONB, updated_by, updated_at | 课表 |
| 分片库 | lock_audit | id, sn, action(lock/unlock/temp_unlock/expire), actor, source, reason, created_at | 锁定留痕 |
| 分片库 | job_queue | queue_id PK, org_id, site_id, name, class_name, auto_approve_materials JSONB, device_sns TEXT[], created_by | 队列 |
| 分片库 | queue_item | item_id PK, queue_id, submitter_user_id, file_sha256, material_id, param_profile_id, status, position, approved_by, rejected_reason, assigned_sn, job_id, submitted_at, started_at, finished_at, skip_count | 队列项 |
| 分片库 | org_alarm_subscription | org_id, site_id, user_id, channels | 告警订阅 |

### 6.3 数据归属与保留

- 组织数据（org / site / member）长期保留；成员 removed_at 软删。
- lock_audit 与 queue_item 保留 2 年，按月分区。
- 设备解除组织归属后，该设备在组织内的 job_record 与 queue_item 留在组织（属于组织的教学记录），设备本身的遥测按 BL1 规则归 SN。
- 学生为未成年人的可能性高：student 角色只存 user_id，不采集任何额外个人信息；提交的文件走 BL4 预签名 URL 1 h 不持久化。

---

## 07 非功能需求与 SLO

| 维度 | 要求 |
|---|---|
| 看板加载 | 200 台站点 P99 < 2 s；聚合计数 P99 < 500 ms |
| 策略生效 | 课表变更 → 在线设备 lock 变化 ≤ 60 s；离线设备上线 ≤ 10 s 内收敛 |
| 本地兜底 | 断网状态下窗口切换准时 ±60 s（RTC 精度） |
| 解锁 | 教师解锁 → 设备可开始 P99 < 5 s（在线） |
| 队列调度 | 设备空闲 → 任务下发 ≤ 30 s；调度器单点故障恢复 ≤ 5 min 且不重复下发 |
| 批量 OTA | 只在窗口内下发；窗口内每站点并发下载 ≤ 5 台（校园带宽） |
| 租户隔离 | 任何跨 org 数据访问为 0；越权测试进 CI |
| 安全零依赖 | 禁用、告警、急停、安全事件、单台 OTA 不依赖 fleet-svc 与 entitlement |
| 规模 | 单组织 ≤ 2000 台，单站点 ≤ 200 台；平台 1 万组织 |

---

## 08 埋点与指标口径

| 指标 | 埋点 / 数据源 | 口径说明 |
|---|---|---|
| 单校设备数 | device_org 按 org 计数 | 中位数；试点期报均值与中位数 |
| 看板周活跃率 | BFF 看板路由访问日志按 user_id 去重 | 一周 ≥ 3 次 |
| 时段外违规开机率 | LOCKED_START_DENIED 事件 + 禁用时段内 JOB_START | 分母为禁用时段数 × 设备数 |
| 禁用误伤安全率 | 禁用期间安全事件未产生告警或未停机 | 对账：events 安全码 vs alarm 表，与 BL1 INC-10 同口径 |
| 队列平均等待 | queue_item.submitted_at → started_at | 中位数；剔除退回与取消 |
| 批量 OTA 窗口内完成率 | ota_device_task 终态时间落在窗口内的比例 | 按批次 |
| 续费率（P2） | subscription org 维度 | 到期 90 天窗口 |

---

## 09 里程碑与验收出口

| 阶段 | 时间 | 交付 | 决策门 |
|---|---|---|---|
| 试点准备 | BL4 P0 出口后 1 个迭代 | 组织模型、成员角色、设备归属、BFF 租户隔离；固件 DR-501 到 DR-504 进排期 | 越权测试进 CI；固件红线锁定排期 |
| P0 试点 | 2 个迭代 | 站点看板、课表锁定与解锁、班级队列、批量 OTA 窗口、告警订阅 | 3 所学校各 ≥ 8 台运行 1 个月：违规开机率 < 1%、禁用误伤安全率 0、队列等待 ≤ 15 min |
| P1 扩展 | 2 个迭代 | 工作室调度、集采清单、SSE、日历、设备转移 | 首个工作室客户 ≥ 12 台 |
| P2 商业化 | entitlement 与支付就位后 | 组织订阅、宽限期、提醒 | FR-562 安全零依赖经代码审查签字 |

**P0 验收清单**：① 越权测试：A 组织成员访问 B 组织全部路由 100% 404；② 禁用台架：locked 下火焰停机 ≤ 100 ms、急停有效、告警推送到达；③ 断网课表切换准时；④ 教师解锁 P99 < 5 s 且审计可查；⑤ 队列端到端：提交 → 审批 → 空闲下发 → JOB_DONE → 排位更新；⑥ 批量 OTA 窗口外零下发、窗口内熔断生效；⑦ 学生角色零个人信息采集经数据字典审计。

---

## 10 依赖、风险与开放问题

### 10.1 依赖

- **BL1 平台**：deviceapi desired 新字段 lock / lock_expires_at / schedule；ota-svc 批次增加 policy.window；alarm-svc 不改，订阅关系在 fleet-svc。
- **BL4**：BFF 鉴权框架扩展 org 维度；云端下发任务 job_start（FR-414）必须先通过安全评审，队列调度依赖它；entitlement（P2）。
- **BL3**：consumable_health 数据用于集采汇总。
- **固件**：DR-501 到 DR-504，其中 locked 不影响安全与本地课表兜底是红线。
- **法务与学校**：学生数据最小化、试点协议、未成年人相关合规意见。
- **App / XCS 团队**：组织入口、讲台看板模式、队列提交与审批界面。

### 10.2 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 禁用逻辑误伤安全功能 | 安全事故，且发生在学校 | DR-501 红线台架测试；禁用只作用于「开始新任务」一个动作；安全链路与 fleet-svc 零依赖 |
| 本地时钟漂移导致上课时锁死 | 教学中断 | RTC 漂移 > 10 min 以云端最后指令为准；教师解锁始终可用；屏幕解锁码（P1） |
| 租户越权看到其他学校 | 隐私与合规 | 所有查询带 org_id；BFF 路由组级中间件；越权测试进 CI |
| 队列调度器重复下发同一任务 | 材料报废 | queue_item 状态机条件 UPDATE；设备端单任务约束；job_start 幂等 |
| 批量 OTA 校园带宽打满 | 网络瓶颈影响教学 | 站点并发 ≤ 5；只在维护窗口 |
| 个人绑定与组织归属冲突处理不清 | 用户困惑与投诉 | 组织优先 + 留痕 + 通知；个人 App 明确显示「由组织管理」 |
| 学生数据合规 | 法律风险 | 只存 user_id；文件不持久化；试点前法务意见 |

### 10.3 开放问题

1. 学生角色是否允许局域网直连绕过队列直接加工？建议由组织策略决定，默认试点学校关闭。
2. 组织归属后个人绑定用户是否保留告警推送？建议保留（多一个人知道机器停了不是坏事），但不保留控制权。
3. 教师解锁上限 240 分钟是否合理，是否允许 org_admin 设置更长的「活动日」解锁。
4. 队列调度是否需要考虑材料库存（同一种材料多少张）。
5. 集团多校区的付费是按组织还是按父组织计。

---

*教育与 B 端 AIoT PRD v0.1 · BL5 复用 BL1 平台与 BL4 入口，只新增组织租户、策略下发与任务队列；禁用绝不影响安全与急停是本文唯一不可协商的约束。待校准项：试点学校清单与设备数、固件 DR-501 到 DR-504 排期、job_start 安全评审结论。*
